interface:
  display_name: "Qdrant"
  short_description: "Repo-specific patterns and workflows for qdrant"
  default_prompt: "Use the qdrant repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true